export {
    default as Link,
    GatsbyLinkProps,
    navigate,
    navigateTo,
    push,
    replace,
    withPrefix,
    withAssetPrefix,
  } from "gatsby-link";
  
export * from "./fakeQL";

// If we need to export more from gatsby, then import it 
// from the full relative path to the node module